spring:
  profiles:
    active: test

---
spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        show_sql: true
    database-platform: org.hibernate.dialect.H2Dialect

    logging:
      level:
        org:
          hibernate:
            orm:
              jdbc:
                bind: TRACE
          springframework:
            transaction:
              interceptor: TRACE

